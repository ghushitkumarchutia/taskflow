{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow - Distributed Background Job Processing Engine & Dashboard",
  "type": "module",
  "scripts": {
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client",
    "start:api": "npm run start:api --prefix server",
    "start:worker": "npm run start:worker --prefix server",
    "dev:api": "npm run dev:api --prefix server",
    "dev:worker": "npm run dev:worker --prefix server",
    "dev:client": "npm run dev --prefix client",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "test:server": "npm run test --prefix server"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
